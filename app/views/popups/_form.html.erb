<%= form_with(model: popup) do |form| %>
  <% if popup.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(popup.errors.count, "error") %> prohibited this popup from being saved:</h2>

      <ul>
        <% popup.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :name, style: "display: block" %>
    <%= form.text_field :name %>
  </div>

  <div>
    <%= form.label :time, style: "display: block" %>
    <%= form.number_field :time %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
